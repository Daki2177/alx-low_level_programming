#include "main.h"

/**
 * _isupper - check if letter is upper
 * @n: number to be computed
 * Return: returns 1 for upper and 0 for anything else
 */

int _isupper(int n)
{
if (n >= 65 && n <= 90)
{
return (1);
}
return (0);
}
